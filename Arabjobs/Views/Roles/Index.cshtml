@model IEnumerable<Microsoft.AspNet.Identity.EntityFramework.IdentityRole>

<p>
    @*@Html.ActionLink("Create New", "Create",new {clas})*@
    <a href="@Url.Action("Create")" class="btn btn-success">
        <span class="glyphicon glyphicon-plus"> </span>
        اضافة جديد
    </a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Name)
        </td>
        <td>
            @Html.ActionLink("تعديل", "Edit", new { id=item.Id}, new { @class = "btn btn-success" }) |
            @Html.ActionLink("تفاصيل", "Details", new { id=item.Id },new { @class = "btn btn-success" }) |
            @Html.ActionLink("حذف", "Delete", new { id=item.Id }, new { @class = "btn btn-success" })
        </td>
    </tr>
}

</table>
